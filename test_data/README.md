# Test data

`sample_test.gpx` is a small GPX 1.1 file containing:

- one waypoint;
- one route with two route points;
- one track with three track points;
- elevation and timestamp values.

It may be used to confirm that the plugin can detect and convert the common
GPX sublayers. Test outputs should be written to `test_output/`, which is
ignored by Git.
